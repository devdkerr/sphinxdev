Dockerized Sphinx Build Environment
========================================

A dockerized Sphinx documentation build environment.

Docker Run
----------------------------------------

```bash
docker run -it -v docprj:/root/docs devdkerr/sphinxdev
```
